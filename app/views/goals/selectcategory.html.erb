<div>
  <div>
    <h2>
      Create Goal
    </h2>


    <div class="row mt-1">

    <% @list_of_goal_categories.each do |a_category| %>
      <div class="col-md-6 text-center">
        <div class="row mt-2 homebtncol">
          <div class="card addcard">
            <div class="card-body">
              <a href="/creategoal/<%=a_category.category%>"><h6 class="mt-2"><i class="<%=a_category.icon%> fa-3x"></i></h6></a>
              <a><%=a_category.category%></a>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    </div>

